#name: WalletApp CI/CD
#
#on:
#  pull_request:
#    branches:
#      - master
#
#jobs:
#  build-and-push:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#
#
#      - name: Set up JDK 21
#        uses: actions/setup-java@v4
#        with:
#          java-version: '21'
#          distribution: 'temurin'
#          cache: 'maven'
#
##      - name: Lint code
##        run: mvn checkstyle:check
#
#      - name: Build JAR
#        run: mvn clean package -DskipTests
#
#      - name: Upload JAR artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: walletapp-jar
#          path: target/*.jar
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build & push Docker image
#        run: |
#          IMAGE=${{ secrets.DOCKER_USERNAME }}/walletapp:latest
#          docker build -t $IMAGE .
#          docker push $IMAGE
#
#  deploy:
#    runs-on: ubuntu-latest
#    needs: build-and-push
#
#    steps:
#      - name: Deploy to VPS
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          script: |
#            cd ~/walletapp
#
#            IMAGE=${{ secrets.DOCKER_USERNAME }}/walletapp
#
#            echo "Pulling latest Docker image..."
#            docker pull $IMAGE:latest
#
#            # Stop and remove existing WalletApp container if it exists
#            if [ "$(docker ps -q -f name=walletapp)" ]; then
#              docker stop walletapp
#              docker rm walletapp
#            fi
#
#            # Start WalletApp container
#            docker run -d \
#              --name walletapp \
#              --network walletapp-network \
#              -p 8080:8080 \
#              -e SPRING_DATA_MONGODB_URI=mongodb://wallet-mongo:27017/sunduk-pay \
#              --restart unless-stopped \
#              $IMAGE:latest