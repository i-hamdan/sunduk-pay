version: '3.8'
services:
  mongodb:
    image: tayyabk/custom-mongo:latest  # or mongo:6.0
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - ./mongo-data:/data/db
